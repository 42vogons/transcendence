FROM node:lts-alpine

ARG BACK_DOMAIN
ENV BACK_DOMAIN=$BACK_DOMAIN

ARG INTRA_URL_TOKEN
ENV INTRA_URL_TOKEN=$INTRA_URL_TOKEN

ARG INTRA_URL_PROFILE
ENV INTRA_URL_PROFILE=$INTRA_URL_PROFILE

WORKDIR /srcs

COPY ./ ./

RUN npm install
